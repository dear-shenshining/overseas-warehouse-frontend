-- ============================================================
-- 删除未使用的索引
-- 根据索引使用情况分析，删除长期未使用的索引以节省空间
-- ============================================================

-- 1. 删除未使用的更新时间索引（节省 112 kB）
-- 说明：当前查询中没有使用 ORDER BY updated_at DESC
DROP INDEX IF EXISTS idx_post_searchs_updated_at;

-- 2. 删除未使用的转单日期索引（节省 48 kB）
-- 说明：如果确定不需要按转单日期查询，可以删除
-- 注意：如果未来需要按转单日期查询，可以重新创建
DROP INDEX IF EXISTS idx_transfer_date;

-- ============================================================
-- 说明
-- ============================================================
-- 1. 删除索引前，请确认这些索引确实不需要
-- 2. 删除索引不会影响数据，只是会影响查询性能
-- 3. 如果未来需要这些索引，可以重新创建
-- 4. 删除索引后，可以节省约 160 kB 的存储空间
-- ============================================================

-- ============================================================
-- 可选：删除使用较少的索引（谨慎操作）
-- ============================================================

-- 如果订单号查询功能不常用，可以考虑删除（节省 40 kB）
-- DROP INDEX IF EXISTS idx_order_num;

-- ============================================================
-- 删除索引后，执行以下命令更新统计信息
-- ============================================================
-- VACUUM ANALYZE post_searchs;

